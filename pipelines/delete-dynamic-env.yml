---
version: '1.0'

steps:
  build:
    type: build
    image_name: codefresh/cf-env-creator
  delete_disposable_env:
    image: ${{build}}
    commands:
      - bash -c -x "bin/delete-dynamic-env $NAMESPACE_NAME $PROJECT_NAME $CLUSTER_NAME $ZONE"

