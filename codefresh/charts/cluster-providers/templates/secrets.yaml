apiVersion: v1
kind: Secret
metadata:
  name: {{ template "fullname" . }}
  labels:
    app: {{ template "fullname" . }}
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
type: Opaque
stringData:
  client-id: {{ .Values.gcAuthClientID }}
  client-secret: {{ .Values.cfAuthClientSecret }}
  mongo-uri: {{ .Values.mongoURI }}
  safe-secret: {{ .Values.safeSecret }}

