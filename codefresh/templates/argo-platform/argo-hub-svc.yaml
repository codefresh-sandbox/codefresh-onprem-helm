apiVersion: v1
kind: Service
metadata:
  name: argo-hub-platform
spec:
  type: "ClusterIP"
  ports:
  - name: "http"
    port: 80
    protocol: TCP
    targetPort: 3000
  selector:
    app: argo-hub-platform